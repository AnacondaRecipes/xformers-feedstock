# CUDA variant compiles 10 GPU architectures against pytorch 2.10.0
# (gpu_cuda128/gpu_cuda130), and the 0.0.34 sdist bundles flash-attention v3
# (hopper/ CUDA sources) which adds substantial compile time — needs an extended
# timeout to avoid being killed at the default PBP build timeout. 12 hours
# matches the onnxruntime CUDA build budget.
task_timeout: 43200  # 12 hours
